import * as React from 'react'
import { Routes, Route, Link, useNavigate } from 'react-router-dom';

export const AdminSidebar: React.FunctionComponent<{}> = (props) =>  {
    return (
        <> 
        Hello From Admin Sidebar
        </>
    );
}